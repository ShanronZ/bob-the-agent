[build]
  command = "npm run build"
  publish = "dist"

# Single-page app — every path serves index.html so a refresh never 404s.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
